// eslint-disable-next-line import/no-unassigned-import
import "../options-storage.js";

console.info("background worker", chrome.runtime);

// const connection = chrome.runtime.connect();

// chrome.runtime.onMessage.addListener(function (request, sender, sendResponse) {
//   console.info("onMessage background ", request, sender, sendResponse);
// });

// chrome.runtime.onMessageExternal.addListener(function (
//   request,
//   sender,
//   sendResponse
// ) {
//   console.info("onMessageExternal background ", request, sender, sendResponse);
// });

// chrome.runtime.onConnect.addListener(function (request, sender, sendResponse) {
//   console.info("onConnect background ", request, sender, sendResponse);
// });

// chrome.runtime.onConnectExternal.addListener(function (
//   request,
//   sender,
//   sendResponse
// ) {
//   console.info("onConnectExternal background ", request, sender, sendResponse);
// });
